# ProCyclingResults

Small Node app that renders a live HTML page for recent pro-cycling results and upcoming top-tier races.

## Run

```bash
npm start
```

Then open `http://localhost:3000`.

## Endpoints

- `/` renders the HTML page.
- `/api/races` returns the aggregated JSON payload.
